import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginRight: 10,
  },
  content: {
    backgroundColor: '#390000',
    borderRadius: 10,
    width: 180,
    height: 110,
  }
});